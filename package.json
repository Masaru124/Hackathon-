{
  "name": "scamshield-ai",
  "version": "1.0.0",
  "description": "AI-powered scam detection system with blockchain registry",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../ai-model && npm install && cd ../blockchain && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ai\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:ai": "cd ai-model && npm start",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "deploy:contract": "cd blockchain && npx hardhat run scripts/deploy.js --network polygon_testnet"
  },
  "keywords": ["scam", "detection", "ai", "blockchain", "phishing", "security"],
  "author": "ScamShield AI Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
